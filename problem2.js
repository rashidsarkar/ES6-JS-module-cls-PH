const frindsWithJor = (arr) => {
  let newArr = [];
  for (name of friends) {
    if (name.length % 2 == 0) {
      newArr.push(name);
    }
  }
  return newArr;
};
const friends = [
  "sabbir",
  "karim,",
  "rahim",
  "nabila",
  "toma",
  "salma",
  "himu",
];
console.log(frindsWithJor(...friends));

const myMathWork = (arr) => {};
